<h2><%= @task.title %></h2>
<p><b><%= @task.details %></b></p>
<p>Has the task been completed? <%= @task.completed %></p>
<%= link_to "Delete", task_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<%= link_to "Edit", edit_path %>
<%= link_to "Dashboard", tasks_path %>
